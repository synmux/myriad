procs:
  _github:
    shell: "cd _github; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  ambio:
    shell: "cd ambio; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  ambio-systems:
    shell: "cd ambio-systems; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  badreputation-org-uk:
    shell: "cd badreputation-org-uk; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  bunnypmsl:
    shell: "cd bunnypmsl; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  dave-io:
    shell: "cd dave-io; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  daveio:
    shell: "cd daveio; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  dcw-soy:
    shell: "cd dcw-soy; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  dotfiles:
    shell: "cd dotfiles; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  dotfiles-fish:
    shell: "cd dotfiles-fish; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  genderbase:
    shell: "cd genderbase; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  homebrew-tap:
    shell: "cd homebrew-tap; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  infrastructure:
    shell: "cd infrastructure; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  loop:
    shell: "cd loop; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  multipost:
    shell: "cd multipost; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  myriad:
    shell: "cd myriad; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  npm:
    shell: "cd npm; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  omi:
    shell: "cd omi; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
  vscode-file-nesting:
    shell: "cd vscode-file-nesting; git fetch --all --prune --tags --prune-tags --recurse-submodules=yes; git pull --all --prune --rebase"
